{
    "name": "complex-query",
    "description": "This runs the same complex query",
    "default": false,
    "schedule": [
    {
      "operation": "complex-query-facettes",
      "warmup-iterations": 10,
      "iterations": {{iterations | default(500)}},
      "clients": {{query_clients | default(1)}}
    }
  ]
}